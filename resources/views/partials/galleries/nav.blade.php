<div class="text-center">
    <a href="{{ route('portfolio.index') }}" class="btn btn-link">Fotos ({{ count($medias) }})</a>
    <a href="{{ route('galleries.index') }}" class="btn btn-link">Galleries ({{ count($galleries) }})</a>
</div>
